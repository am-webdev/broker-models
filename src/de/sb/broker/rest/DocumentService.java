package de.sb.broker.rest;

import javax.ws.rs.Path;

@Path("documents")
public class DocumentService {
	
	/* TODO GET and PUT
	 * Check of these methods needs to be implemented into PersonService
	 * 
	 * the correct path should be:
	 * 		GET person/{:identity}/avatar (response: content+mimetype)
	 * 		PUT person/{:identity}/avatar (check if hash does already exist.)
	 * 
	 * But we can implement some basics to get something like:
	 * 
	 * 		GET documents/{:identity}
	 * 		PUT documents/{:identity}
	 */

}
